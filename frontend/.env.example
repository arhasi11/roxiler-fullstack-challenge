The full base URL for your backend API.
This is how the frontend knows where to send all login, signup, and data requests.
It should point to the address where your backend server is running.
VITE_API_URL=http://localhost:4000/api